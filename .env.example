# Copy to .env at repository root. Commands load it without overriding existing environment.
DATABASE_URL=postgresql://platform:local-development-only@localhost:5432/platform
API_HOST=127.0.0.1
API_PORT=4000

